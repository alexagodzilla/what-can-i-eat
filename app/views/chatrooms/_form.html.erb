 <%= form_with model: [chatroom, message], class: "chatroom-form",
  html: { data: { action: "turbo:submit-end->chatroom-subscription#resetForm" } } do |f| %>
  <%= f.text_field :content, placeholder: "Message ##{ chatroom.name }" %>
  <%= f.submit "Send" %>
<% end %>
